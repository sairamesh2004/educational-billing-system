# Database

This project uses Oracle Database XE.

The database is not included in this repository.

Before running the application:

- Install Oracle Database XE.
- Create the required database tables.
- Update the JDBC connection details if needed.

## Required Tables

- EmployeeDetails
- StudentDetails

The project uses JDBC to perform CRUD operations on these tables.
